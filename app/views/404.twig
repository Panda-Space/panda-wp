{% extends '_html.twig' %}

{% block body %}
  <section class="c-section c-section--404">
    <div class="container py-5">
      <h1 class="fs-25 text-center pri-color w-sbold mb-3">Error 404</h1>
      <p class="mb-5 text-center">Página no encontrada</p>
      <div class="flex flex-column align-items-center pt-3 mb-2">
        <figure class="c-pic mb-4">
          <img src="{{theme.link}}/static/public/{{image404}}" alt="">
        </figure>

        <a href="{{ site.url }}" class="c-button c-button--primary d-inline-block">
          Ir al inicio
        </a>
      </div>
    </div>
  </section>
{% endblock %}
